MORTY69 SERVICES — PYTHON VERSION (Render)
============================================

STEP 1 — Push updated files to GitHub
--------------------------------------
Open PowerShell in the "Mortys site" folder:

  cd "C:\Users\Kenny\Mortys site"
  git add .
  git commit -m "Switch to Python for Render"
  git push

STEP 2 — Render settings (Python 3)
-------------------------------------
Go to Render Dashboard → your service → Settings

  Runtime:        Python 3
  Build Command:  pip install -r requirements.txt
  Start Command:  gunicorn app:app --bind 0.0.0.0:$PORT

Environment variables:
  ADMIN_KEY       = Morty666
  DISCORD_WEBHOOK = https://discord.com/api/webhooks/1538332569102319616/-Ltcb2L4u0oEiHPz4e10_WqgkBf0NJFdhrkjXsLdxPperMCH-4WSPgSjkPFsReAkp313

Disk (important — keeps orders & product images):
  Mount path: /opt/render/project/src/data
  Size: 1 GB

Then: Manual Deploy → Deploy latest commit

FILES YOU NEED ON GITHUB
------------------------
Mortys site/
  app.py              ← main Python server
  requirements.txt    ← Python packages
  render.yaml
  .gitignore
  README.md
  DEPLOY.txt
  data/
    uploads/
  public/
    index.html
    css/style.css
    js/app.js

Admin key: Morty666
